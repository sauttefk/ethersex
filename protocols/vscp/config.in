if [ "$NET_MAX_FRAME_LENGTH" -gt 571 ]; then
  define_bool NET_MAX_FRAME_LENGTH_GT_571 y
else
  define_bool NET_MAX_FRAME_LENGTH_GT_571 n
fi

dep_bool_menu "VSCP Node" VSCP_SUPPORT $NET_MAX_FRAME_LENGTH_GT_571 $UDP_SUPPORT
  int "UDP Port" CONF_VSCP_PORT 9598
  comment "Universe Settings"
  comment  "Debugging Flags"
  dep_bool 'VSCP' DEBUG_VSCP $DEBUG
endmenu
